# ==========================================
# HumanityOS Frontend Environment Variables
# ==========================================

# Base URL to point to backend services
# In production, this can be the Cloud Run service URL or custom domain API path
# In development (Docker Compose environment), it will resolve relative proxy or direct localhost
VITE_API_URL=http://localhost:8080/api/v1
